Сделать Android-приложение, которое:

Показывает попап-напоминание каждые 30 минут с 9:00 до 22:00

Показывает задачи: временные (по часам) и общие (повторяющиеся)

Не показывает попап, если все общие задачи выполнены и нет задач на текущее время

Позволяет вручную редактировать список задач в виде текста

Поддерживает режим «Я работаю», отключающий напоминания на 1 час

Попап не появляется, если телефон не в руках (экран выключен или заблокирован)

📁 Что нужно реализовать
1. Редактор задач
На главном экране кнопка Редактировать

Открывается EditText, куда пользователь вручную вводит задачи, построчно

Формат:

09:00 выпить воду — временная задача

15 мин читать Medium — общая задача

Сохраняется локально (SharedPreferences)

На каждый день — список общих задач сбрасывается (выполнено/не выполнено)

2. Планирование напоминаний
Таймер запускается каждые 30 минут с 9:00 до 22:00

Проверяет:

Активен ли режим “Я работаю”

Включён ли экран и не заблокирован ли телефон

Есть ли задача на текущее время или невыполненные общие задачи

Только если всё ОК → запускает PopupActivity

3. Попап (полноэкранное окно)
Отображает задачи:

Если сейчас точное время задачи → показывается она

Плюс все ещё невыполненные общие задачи

Кнопка ✅ «Выполнено»:

Помечает текущие общие задачи как выполненные

Закрывает окно

Кнопка 🔁 «Отложить»:

Окно закрывается

Через 5 минут повторное напоминание с теми же задачами

4. Режим “Я работаю”
На главном экране — переключатель

При включении:

Попапы не запускаются в течение 1 часа

Автоматически выключается через 60 минут

Отображается текст: “Режим активен до 14:30”

5. Условие “телефон в руке”
Перед каждым запуском попапа — проверка:

PowerManager.isInteractive == true

KeyguardManager.isKeyguardLocked == false

Если экран выключен или телефон заблокирован → пропускаем

🧠 Хранилище:
SharedPreferences:

tasks_raw (весь список в текстовом виде)

list of completed general task IDs for today

timestamp до которого активен “Я работаю”

🚫 Не делать в MVP:
Telegram, облако, токены, геймификацию, мультяшный UI, анимации